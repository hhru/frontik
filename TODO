# -*- mode: org -*-

* unittest на формирование XML
* комментарий с URL перед каждым блоком полученным по HTTP
* frontik: если не получается получить сессию - плохое сообщение об ошибке
* таймауты
** таймаут по умолчанию
* вставка xml из файла
* правильная выдача для /article/
* daemon.pidlockfile
* frontik -> frontik_srv
* frontik_www -> frontik
* пострелять по frontik 
